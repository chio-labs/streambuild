name = "orders_demo"
default_target = "demo"

[defaults]
pipeline_mode = "direct"
kafka_broker_list = "redpanda:9092"

[connection]
host = "localhost"
port = 18123
username = "clickhouse"
password = "clickhouse"

[targets.demo]
database = "orders_demo"
